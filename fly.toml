# fly.toml - hr-bot Fly.io設定

app = 'hr-bot'
primary_region = 'nrt'

[build]

# 最小スペックのVM（無料枠内）
[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"

# SQLiteファイルを永続化するボリューム
[[mounts]]
  source = "hr_data"
  destination = "/data"
